# Copy this file to .devcontainer/.env (gitignored) and fill in your key before
# opening the container. The devcontainer loads it via `--env-file`, so the key
# is injected at run time and never baked into the image or committed to git.
ANTHROPIC_API_KEY=
